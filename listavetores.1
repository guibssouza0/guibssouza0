#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	FILE* f1=fopen("numeros.txt", "r+t");
	if(f1==NULL){
		printf("erro ao abrir arquivo");
		exit(1);
}
	int arranjo[1000];
	for(int i=0;i<100;i++)
  {
	   fscanf(f1, "%d", &arranjo[i]); 
	}
	int p;
	printf("voce quer saber o valor de qual elemento? ");
	scanf("%d", &p);
	printf("o valor do elemento %d eh: %d\n", p, arranjo[p]);
	fclose(f1);
	return(0);
}
